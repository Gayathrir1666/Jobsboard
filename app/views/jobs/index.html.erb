<h1>Jobs</h1>

<% @jobs.each do |job| %>
  <div class="job">
    <h6><%= link_to job.title, job.url %></h6>
    <p><%= job.company %></p>
  </div>
<% end %>

<p><%= link_to "Add a job", new_job_path %></p>
<p><%= paginate @jobs %></p>
